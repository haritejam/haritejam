import { ResetLocalClient } from "@/components/reset-local-client";

export const metadata = { title: "Clear local data | FlexiDine" };

export default function ResetLocalPage() {
  return <ResetLocalClient />;
}
