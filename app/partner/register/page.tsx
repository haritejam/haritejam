import { PartnerRegister } from "@/components/partner-register";

export const metadata = {
  title: "Restaurant onboarding | FlexiDine",
  description: "Onboard your restaurant for FlexiDine pre-order, BOPIS, and FlexiSwitch.",
};

export default function PartnerRegisterPage() {
  return <PartnerRegister />;
}
